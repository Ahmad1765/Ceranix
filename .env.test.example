# Playwright e2e credentials — gitignored.
# Used by tests/e2e/auth.setup.ts to drive a real UI sign-in once per run.
# All authenticated specs reuse the storage state this setup produces.

EXPO_PUBLIC_SUPABASE_URL=https://dummy-project-id.supabase.co
EXPO_PUBLIC_SUPABASE_ANON_KEY=dummy-anon-key-placeholder

E2E_USER_EMAIL=dummy-e2e-user@example.com
E2E_USER_PASSWORD=dummy-e2e-password
